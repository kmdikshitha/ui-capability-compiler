# ui-capability-compiler
Discover a legacy UI flow with an LLM once, compile it into a typed versioned capability, replay it deterministically with no model in the loop with real human handoff on the same live session.
